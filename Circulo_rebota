var x;
var y;
function setup() {
  createCanvas(400, 400);
	x=0;
  y=0;
}

function draw() {
	background(220);
	fill(0,0,255);
  ellipse(x,200,10,10);
      y++;
    if((y>=400) && (y<=800)){
    x--;
	   if(y>=801){
      y=0;
    }
    }
  else{
  	x++;
	   if(y>=801){
      y=0;
    }
 	}
}
